#!/bin/zsh

if [[ "/home/ksan/bin/$1" == $(which $1) ]]; then
	echo "Are you sure you want to delete $1? y/n" 
	read confirm
	mv $HOME/bin/.zipped $HOME/bin/.notzipped
else
	echo "Cmd not exits."
fi


if [[ $confirm == 'y' ]]; then
	rm -f $HOME/bin/$1
fi
